#!/usr/bin/env bash

#echo "Instalando Puppet en modo de configuracion standalone..."
#En boxes hashicorp/precise32 o 64 solo instala puppet 2.7
#apt-get update
#apt-get install -y puppet

echo "Puppet3 viene preinstalado en el box de puppet-labs."